apiVersion: v1

values:
  images:
    snapshotController: gcr.io/k8s-staging-sig-storage/snapshot-controller:v5.0.0

patches:
  values.yaml:
    snapshot_controller_image: "{{ registryFrom .images.snapshotController }}/{{ repositoryFrom .images.snapshotController }}:{{ tagFrom .images.snapshotController }}"